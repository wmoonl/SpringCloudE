<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="club.comm.dao.GoodsDao">

    <!-- create user -->
    <!--<select id="queryUsers" resultType="club.comm.domain.UserModel">
        select * from user
    </select>-->

    <!-- update user
    <update id="updateUser" parameterType="com.example.demo.domain.UserModel">
        UPDATE user
        SET realname = #{realName}, phonenumber = #{phoneNumber}, sex = #{sex}
        WHERE
        username = #{userName}
    </update>

    &lt;!&ndash; find user &ndash;&gt;
    <select id="queryUser" parameterType="String" resultType="com.example.demo.domain.UserModel">
        SELECT
        id, username, realname, password, salt, sex, phonenumber, createtime
        FROM
        user
        WHERE
        username = #{userName}
    </select>

    <select id="validate" parameterType="String" resultType="com.example.demo.domain.UserModel">
        SELECT
        id, username, realname, password, salt, sex, phonenumber, createtime
        FROM
        user
        WHERE
        username = #{userName} and password = #{password}
    </select>

    &lt;!&ndash; update password &ndash;&gt;
    <update id="updatePassword" parameterType="String">
        UPDATE
        user
        SET
        password = #{password},
        salt = #{salt}
        WHERE
        username = #{userName}
    </update>-->
</mapper>